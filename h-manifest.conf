CUSTOM_NAME="${CUSTOM_NAME:-keryx-miner-v0.1.4.7-OPoI}"
CUSTOM_VERSION="${CUSTOM_VERSION:-0.1.4.7}"
CUSTOM_ALGO="${CUSTOM_ALGO:-keryxhash}"
CUSTOM_COIN="${CUSTOM_COIN:-KRX}"
CUSTOM_LOG_BASENAME="${CUSTOM_LOG_BASENAME:-${CUSTOM_LOGBASE:-keryx-miner}}"
CUSTOM_CONFIG_FILENAME="${CUSTOM_CONFIG_FILENAME:-config.ini}"
CUSTOM_CONFIG_PARSER="${CUSTOM_CONFIG_PARSER:-h-config.sh}"
CUSTOM_MINER_BIN="${CUSTOM_MINER_BIN:-${CUSTOM_MINERBIN:-keryx-miner}}"
CUSTOM_MINER_API_PORT="${CUSTOM_MINER_API_PORT:-4068}"
CUSTOM_URL="${CUSTOM_URL:-stratum+tcp://krx.suprnova.cc:4404}"

# Conventional HiveOS custom miner aliases used by some custom miner flows.
CUSTOM_MINERBIN="$CUSTOM_MINER_BIN"
CUSTOM_LOGBASE="$CUSTOM_LOG_BASENAME"
